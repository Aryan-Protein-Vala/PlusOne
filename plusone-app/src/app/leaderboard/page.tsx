import { createClient } from '@/lib/supabase/server'

export default async function LeaderboardPage(){const s=await createClient();const{data:providers}=await s.from('profiles').select('id,name,city,trust_score,is_verified').eq('role','host').eq('country_code','IN').order('trust_score',{ascending:false}).limit(50);return <div className="app-container" style={{maxWidth:1000,margin:'0 auto',padding:'40px 24px'}}><h1 style={{margin:'0 0 8px',fontSize:36}}>Community leaders</h1><p style={{color:'var(--muted-foreground)',marginBottom:28}}>Trusted hosts across India.</p><div className="app-card" style={{padding:10}}>{(providers||[]).length===0?<p style={{padding:20,color:'var(--muted-foreground)'}}>The leaderboard will appear as hosts complete verified activities.</p>:(providers||[]).map((p:any,i:number)=><div key={p.id} style={{display:'flex',alignItems:'center',gap:16,padding:16,borderBottom:'1px solid var(--border)'}}><strong style={{width:28}}>{i+1}</strong><div style={{flex:1}}><strong>{p.name}</strong><small style={{display:'block',color:'var(--muted-foreground)'}}>{p.city||'India'} {p.is_verified?'· Verified':''}</small></div><strong>{Number(p.trust_score||0).toFixed(0)} trust</strong></div>)}</div></div>}
