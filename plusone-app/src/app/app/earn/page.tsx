import { createClient } from '@/lib/supabase/server'

export default async function EarnDashboardPage(){const s=await createClient();const{data:{user}}=await s.auth.getUser();let listings:any[]=[];let bookings:any[]=[];if(user){const l=await s.from('provider_listings').select('id,title,city,hourly_rate,status').eq('host_id',user.id).order('created_at',{ascending:false});listings=l.data||[];const b=await s.from('bookings').select('id,status,amount,currency').eq('host_id',user.id).order('created_at',{ascending:false}).limit(10);bookings=b.data||[]}const completed=bookings.filter(b=>b.status==='completed').reduce((n,b)=>n+Number(b.amount),0);return <div><div style={{marginBottom:30}}><h1 style={{margin:'0 0 6px',fontSize:32,fontWeight:650}}>Earn by being someone&apos;s PlusOne.</h1><p style={{margin:0,color:'var(--muted-foreground)'}}>Manage your listings, requests, and earnings in one place.</p></div><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(180px,1fr))',gap:14,marginBottom:28}}>{[['Completed earnings',`₹${completed.toLocaleString('en-IN')}`],['Active listings',String(listings.filter(l=>l.status==='published').length)],['Total requests',String(bookings.length)]].map(([label,value])=><div className="app-card" key={label} style={{padding:20}}><span style={{fontSize:12,color:'var(--muted-foreground)'}}>{label}</span><strong style={{display:'block',fontSize:26,marginTop:8}}>{value}</strong></div>)}</div><div className="app-card" style={{padding:22}}><h2 style={{margin:'0 0 16px',fontSize:18}}>Your offers</h2>{listings.length===0?<p style={{color:'var(--muted-foreground)'}}>Create your first listing to start earning.</p>:listings.map(l=><div key={l.id} style={{display:'flex',justifyContent:'space-between',padding:'13px 0',borderBottom:'1px solid var(--border)'}}><span>{l.title}<small style={{display:'block',color:'var(--muted-foreground)'}}>{l.city} · {l.status}</small></span><strong>₹{Number(l.hourly_rate).toLocaleString('en-IN')}/hr</strong></div>)}</div></div>}
